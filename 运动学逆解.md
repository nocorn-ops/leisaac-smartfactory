# 运动学逆解（Inverse Kinematics）实现详解

## 概述

LeIsaac 的逆运动学（IK）基于 IsaacLab 的 `DifferentialInverseKinematicsAction` 实现，核心算法是**阻尼最小二乘法（DLS, Damped Least Squares / Levenberg-Marquardt）**。当你使用键盘、手柄、或状态机（state machine）操控机械臂时，你输入的是**末端执行器的位姿增量**，IK 求解器自动将其转换为**关节角度目标**，然后驱动仿真中的机械臂。

---

## 1. 架构概览

```
用户输入（键盘/手柄）
    ↓
SO101Keyboard.get_device_state()  → 末端位姿增量 (dx,dy,dz,dr,dp,dy)
    ↓
DifferentialInverseKinematicsAction.process_actions()
    ├── 缩放 + 裁剪
    └── IKController.set_command()
        ├── use_relative_mode=True  → apply_delta_pose() 叠加增量到当前末端位姿
        └── use_relative_mode=False → 直接用输入作为绝对目标
    ↓
DifferentialInverseKinematicsAction.apply_actions()
    ├── 从 PhysX 获取当前末端位姿
    ├── 从 PhysX 获取几何雅可比矩阵 J (6×n)
    ├── 计算位姿误差 Δx = x_des - x_curr
    └── IKController.compute()
        └── DLS 求解: Δq = J^T (J J^T + λ²I)^(-1) Δx
    ↓
Δq 加到当前关节角度 → asset.set_joint_position_target()
    ↓
PhysX 物理步进 → 机械臂运动
```

**涉及的三个核心文件：**

| 文件 | 作用 |
|------|------|
| `isaaclab/controllers/differential_ik.py` | DLS/Pinv/SVD/Transpose 四种 IK 求解算法 |
| `isaaclab/controllers/differential_ik_cfg.py` | IK 控制器配置（方法、参数、模式） |
| `isaaclab/envs/mdp/actions/task_space_actions.py` | 将 IK 封装为 Gym 动作项，获取 Jacobian、执行步进 |

---

## 2. 核心算法：DLS（Damped Least Squares）

### 2.1 数学原理

微分逆运动学的基本公式：

$$\Delta \mathbf{q} = \mathbf{J}^{\dagger} \cdot \Delta \mathbf{x}$$

其中：
- $\Delta \mathbf{q} \in \mathbb{R}^n$：关节角度增量
- $\mathbf{J} \in \mathbb{R}^{6 \times n}$：几何雅可比矩阵（映射关节速度到末端速度）
- $\Delta \mathbf{x} \in \mathbb{R}^6$：末端位姿误差（位置 + 轴角旋转）
- $\mathbf{J}^{\dagger}$：雅可比矩阵的（阻尼）伪逆

**DLS (Damped Least Squares)** 的伪逆计算：

$$\mathbf{J}^{\dagger} = \mathbf{J}^T (\mathbf{J} \mathbf{J}^T + \lambda^2 \mathbf{I})^{-1}$$

其中 $\lambda$ 是阻尼系数（LeIsaac 默认 $\lambda = 0.01$，状态机模式 $\lambda = 0.04$）。

**为什么要加阻尼？** 当机械臂接近奇异位姿时，$\mathbf{J}\mathbf{J}^T$ 会变得奇异或接近奇异。不加阻尼时伪逆会数值不稳定，导致关节速度爆炸。$ \lambda^2 \mathbf{I}$ 项保证了矩阵始终可逆。

### 2.2 代码实现

```python
# differential_ik.py 第 227-236 行
elif self.cfg.ik_method == "dls":
    lambda_val = self.cfg.ik_params["lambda_val"]
    jacobian_T = torch.transpose(jacobian, dim0=1, dim1=2)
    lambda_matrix = (lambda_val**2) * torch.eye(n=jacobian.shape[1], device=self._device)
    delta_joint_pos = (
        jacobian_T @ torch.inverse(jacobian @ jacobian_T + lambda_matrix) 
        @ delta_pose.unsqueeze(-1)
    )
    delta_joint_pos = delta_joint_pos.squeeze(-1)
```

### 2.3 四种 IK 方法对比

| 方法 | 公式 | 优点 | 缺点 | 参数 |
|------|------|------|------|------|
| **pinv** | $\mathbf{J}^+ \Delta\mathbf{x}$ | 精确伪逆 | 奇异时不稳定 | `k_val`（增益） |
| **svd** | 截断 SVD 伪逆 | 可过滤小奇异值 | 计算量大 | `k_val`, `min_singular_value` |
| **trans** | $k \cdot \mathbf{J}^T \Delta\mathbf{x}$ | 极快、无条件稳定 | 收敛慢 | `k_val`（增益） |
| **dls** | $\mathbf{J}^T(\mathbf{J}\mathbf{J}^T + \lambda^2\mathbf{I})^{-1}\Delta\mathbf{x}$ | 平滑处理奇异位姿 | 引入微小误差 | `lambda_val`（阻尼） |

LeIsaac **全部使用 DLS**，这是工业机器人 IK 的标准选择。

---

## 3. 雅可比矩阵的来源

在仿真中，雅可比矩阵直接从 PhysX 物理引擎获取：

```python
# 世界坐标系下的几何雅可比
jacobian_w = self._asset.root_physx_view.get_jacobians()[:, body_idx, :, joint_ids]
# 转到机器人基坐标系
jacobian_b = rotate_to_base(jacobian_w)
```

PhysX 内部的刚体动力学引擎可以直接计算任意 body 相对于关节的雅可比矩阵，这是仿真环境独有的优势——**不依赖解析运动学模型**。

---

## 4. LeIsaac 中的 IK 配置

### 4.1 键盘/手柄（相对模式）

```python
# action_process.py
action_cfg.arm_action = mdp.DifferentialInverseKinematicsActionCfg(
    asset_name="robot",
    joint_names=["shoulder_lift", "elbow_flex", "wrist_flex", "wrist_roll"],
    body_name="gripper",
    controller=mdp.DifferentialIKControllerCfg(
        command_type="pose",
        ik_method="dls",
        use_relative_mode=True,  # 增量模式
    ),
)
```

- **相对模式**：每次输入的 6 维向量 (dx,dy,dz,dr,dp,dy) 叠加到当前末端位姿
- **排除 shoulder_pan 和 gripper**：前者单独控制（放到 gripper_action 里），后者由独立的夹爪动作控制

### 4.2 状态机（绝对模式）

```python
# action_process.py — so101_state_machine
action_cfg.arm_action = mdp.DifferentialInverseKinematicsActionCfg(
    ...
    controller=mdp.DifferentialIKControllerCfg(
        command_type="pose",
        ik_method="dls",
        ik_params={"lambda_val": 0.04},  # 更大的阻尼
        use_relative_mode=False,  # 绝对位姿模式
    ),
)
```

- **绝对模式**：输入是末端执行器的绝对位姿（7 维：xyz + 四元数）
- **更大阻尼**：状态机生成的是绝对目标，运动幅度可能较大，用更大的 λ 保证稳定性

### 4.3 两种模式对比

| 模式 | `use_relative_mode` | 输入维度 | 使用场景 |
|------|:---:|:---:|------|
| 相对模式 | `True` | 6 (dx,dy,dz,dr,dp,dy) | 键盘/手柄遥操作 |
| 绝对模式 | `False` | 7 (x,y,z,qw,qx,qy,qz) | 状态机自动生成、策略推理 |

---

## 5. 完整调用链

### 5.1 键盘遥操作的每一步

```
1. SO101Keyboard.advance()
   → SO101Keyboard.input2action()
   → SO101Keyboard.get_device_state()
   → 返回 delta_action[8]: (dx,dy,dz,dr,dp,dy,d_shoulder_pan,d_gripper)

2. env.cfg.preprocess_device_action(action, device)
   → action_process.py/preprocess_device_action()
   → 组装成 8 维 tensor: [dx,dy,dz,dr,dp,dy,shoulder_pan,gripper]

3. env.step(action)
   → ActionManager.process_action(action)
   → DifferentialInverseKinematicsAction.process_actions(action[:6])
      → 缩放: action *= scale
      → 获取当前 ee 位姿
      → IKController.set_command(delta_pose, ee_pos, ee_quat)
         → apply_delta_pose(ee_pos, ee_quat, delta_pose) → ee_pos_des, ee_quat_des
   
   → ActionManager.apply_action()
   → DifferentialInverseKinematicsAction.apply_actions()
      → 获取当前 ee 位姿 + Jacobian
      → IKController.compute(ee_pos, ee_quat, jacobian, joint_pos)
         → compute_pose_error() → position_error(3) + axis_angle_error(3) = Δx(6)
         → DLS 求解: Δq = J^T (J J^T + λ²I)^(-1) Δx
         → joint_pos_des = joint_pos + Δq
      → asset.set_joint_position_target(joint_pos_des)
   
   → JointPositionAction.apply_actions()   # shoulder_pan 直接设
   → JointPositionAction.apply_actions()   # gripper 直接设

4. PhysX 物理步进
5. 渲染 → 下一帧
```

### 5.2 末端误差计算

```python
# math.py/compute_pose_error
position_error = ee_pos_des - ee_pos_curr    # 位置误差 (3,)
# 四元数误差 → 轴角表示
axis_angle_error = quat_to_axis_angle(q_des * q_curr^(-1))  # 旋转误差 (3,)
pose_error = cat(position_error, axis_angle_error)  # (6,)
```

---

## 6. 迁移到真机 LeRobot 的可行性评估

### 6.1 核心挑战：Jacobian 的来源

这是最关键的差异：

| | 仿真 (IsaacLab) | 真机 (LeRobot) |
|------|------|------|
| Jacobian 来源 | PhysX 物理引擎 GPU 计算 | **需要自己算** |
| 机器人模型 | USD/URDF 自动解析 | **需要手动建立运动学模型** |
| 末端位姿 | `body_pos_w + body_quat_w` 直接读 | 需要正运动学（FK）从关节角度推算 |

### 6.2 迁移方案

要将 IsaacLab 的 IK 迁移到真机 LeRobot，需要：

**方案 A：解析雅可比（推荐）**

对于 SO101 这类串联机械臂，可以用 **DH 参数法** 或 **PoE（Product of Exponentials）** 建立运动学模型，然后通过数值或符号微分得到雅可比：

```python
# 伪代码
def compute_jacobian(joint_angles, dh_params):
    """用解析法计算 6×6 几何雅可比"""
    J = torch.zeros(6, 6)
    T = eye(4)
    for i in range(6):
        T_i = dh_transform(dh_params[i], joint_angles[i])
        T = T @ T_i
        z = T[:3, 2]                    # 关节 i 的旋转轴
        p_ee = T_ee[:3, 3] - T[:3, 3]   # 从关节 i 到末端的位置向量
        J[:3, i] = cross(z, p_ee)       # 位置部分
        J[3:, i] = z                     # 旋转部分
    return J
```

**需要准备：**
- SO101 的 DH 参数 （可从 URDF 或 SolidWorks 提取）
- 用 PyTorch 实现 FK + Jacobian（可以完全复用 IsaacLab 的 DLS 求解器）

**方案 B：数值雅可比（简单但慢）**

```python
def numerical_jacobian(joint_angles, fk_func, epsilon=1e-6):
    """用有限差分计算雅可比"""
    J = torch.zeros(6, 6)
    fk_current = fk_func(joint_angles)  # 当前末端位姿
    for i in range(6):
        q_perturbed = joint_angles.clone()
        q_perturbed[i] += epsilon
        fk_perturbed = fk_func(q_perturbed)
        J[:, i] = (fk_perturbed - fk_current) / epsilon
    return J
```

每次 IK 迭代需要 7 次 FK 计算，对于控制频率不高的场景可以接受。

### 6.3 可以直接复用的部分

| IsaacLab 组件 | 可行性 | 说明 |
|------|:---:|------|
| `DifferentialIKController` 的 DLS 求解 | ✅ 直接可用 | 纯 PyTorch，无 Isaac Sim 依赖 |
| `compute_pose_error()` | ✅ 直接可用 | 纯数学，可直接移植 |
| `apply_delta_pose()` | ✅ 直接可用 | 纯数学 |
| `skew_symmetric_matrix()` | ✅ 直接可用 | 纯 PyTorch |
| PhysX `get_jacobians()` | ❌ 不可用 | 需要用解析/数值 Jacobian 替代 |
| `body_pos_w / body_quat_w` | ❌ 不可用 | 需要用 FK 替代 |

### 6.4 完整迁移清单

要实现在真机 LeRobot 上从末端位姿控制逆解出关节角度，需要：

1. **建立运动学模型**：从 URDF 提取 DH 参数 → Python/PyTorch 实现 FK + 解析 Jacobian
2. **移植 IK 求解器**：直接复用 IsaacLab 的 `_compute_delta_joint_pos()`（DLS 方法），只需约 30 行代码
3. **移植位姿运算**：`apply_delta_pose()`, `compute_pose_error()`, `quat_inv()`, `matrix_from_quat()`, `skew_symmetric_matrix()` — 都是纯 PyTorch
4. **封装为推理管道**：输入末端位姿 → 输出关节角度 → 发送给舵机

### 6.5 评估结论

| 维度 | 评估 |
|------|------|
| **可行性** | ✅ 完全可行 |
| **DLS 算法** | ✅ 可直接复用（30 行纯 PyTorch） |
| **数学工具** | ✅ 可直接复用（~200 行纯 PyTorch） |
| **Jacobian** | ⚠️ 需要自己实现（解析法推荐，约 50 行） |
| **运动学模型** | ⚠️ 需要从 URDF 提取 DH 参数 |
| **真机控制频率** | ✅ DLS 在 CPU 上每次求解 <1ms，满足 50-100Hz 要求 |
| **难度** | 中低 — 核心算法已就绪，主要工作在建立运动学模型 |

**总结：IsaacLab 的 DLS IK 算法本质上是纯数学库，可以零依赖迁移到真机 LeRobot。核心工作在于为 SO101 建立解析运动学模型（DH 参数 + FK + Jacobian），IK 求解部分可以直接复制粘贴。**

---

## 7. 参考资料

- [Robot Dynamics Lecture Notes](https://ethz.ch/content/dam/ethz/special-interest/mavt/robotics-n-intelligent-systems/rsl-dam/documents/RobotDynamics2017/RD_HS2017script.pdf) — Marco Hutter, ETH Zurich
- [Introduction to Inverse Kinematics](https://www.cs.cmu.edu/~15464-s13/lectures/lecture6/iksurvey.pdf) — Samuel R. Buss, UCSD
- IsaacLab 源码：`isaaclab/controllers/differential_ik.py`
- IsaacLab 源码：`isaaclab/envs/mdp/actions/task_space_actions.py`
